datasource db {
  provider = "sqlite"
  url      = "file:../../db/sqlite.db"
}

generator client {
  provider = "cargo prisma"
  output   = "../src/db.rs"
  // module_path = "crate::db"
}

model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}
