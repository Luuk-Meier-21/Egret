generator client {
  provider    = "cargo prisma"
  output      = "../src/db.rs"
  module_path = "crate::db"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}
